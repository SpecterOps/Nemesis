{{/*
TLS secret is created by setup-cluster.sh (either from infra/traefik/certs/
or as a self-signed certificate). This file is intentionally empty.

To manually create/update:
  kubectl create secret tls nemesis-tls-secret -n nemesis \
    --cert=tls.crt --key=tls.key --dry-run=client -o yaml | kubectl apply -f -
*/}}
