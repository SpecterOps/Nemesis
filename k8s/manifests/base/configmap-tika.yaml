# This file is generated by deploy.sh which inlines the tika config files.
# Placeholder — deploy.sh creates this from the Helm chart's files/ directory.
apiVersion: v1
kind: ConfigMap
metadata:
  name: tika-config
  namespace: ${NAMESPACE}
  labels:
    app.kubernetes.io/part-of: nemesis
data:
  tika-config.xml: |
    __PLACEHOLDER_TIKA_CONFIG_XML__
  tika-config-tesseract.xml: |
    __PLACEHOLDER_TIKA_CONFIG_TESSERACT_XML__
