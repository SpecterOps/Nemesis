# This file is generated by deploy.sh which inlines rabbitmq config files.
# Placeholder — deploy.sh creates this from the Helm chart's files/ directory.
apiVersion: v1
kind: ConfigMap
metadata:
  name: rabbitmq-config
  namespace: ${NAMESPACE}
  labels:
    app.kubernetes.io/part-of: nemesis
data:
  rabbitmq.conf: |
    __PLACEHOLDER_RABBITMQ_CONF__
  enabled_plugins: |
    __PLACEHOLDER_ENABLED_PLUGINS__
