# This file is generated by deploy.sh which inlines the SQL files.
# Placeholder — deploy.sh creates this from the Helm chart's files/ directory.
apiVersion: v1
kind: ConfigMap
metadata:
  name: postgres-init
  namespace: ${NAMESPACE}
  labels:
    app.kubernetes.io/part-of: nemesis
data:
  01-schema.sql: |
    __PLACEHOLDER_01_SCHEMA_SQL__
  02-seed.sql: |
    __PLACEHOLDER_02_SEED_SQL__
